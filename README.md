# مكتب أضواء الفيصلية للخدمات العقارية

موقع عقاري احترافي متكامل باللغة العربية مع نظام إدارة (لوحة تحكم) للمشرف.

---

## 🚀 طريقة التشغيل

1. افتح ملف `index.html` في المتصفح لعرض الموقع العام
2. افتح ملف `admin.html` للوصول إلى لوحة التحكم

### بيانات الدخول الافتراضية للوحة التحكم:
```
اسم المستخدم: admin
كلمة المرور:  admin123
```

> **⚠️ تنبيه أمني:** سيتم إجبارك على تغيير كلمة المرور عند أول دخول.

---

## 📁 هيكل المشروع

```
موقع-المكتب-العقاري/
├── index.html              # الموقع العام (الواجهة للزوار)
├── admin.html              # لوحة التحكم (للمشرف)
├── css/
│   ├── style.css           # تنسيقات الموقع العام
│   └── admin.css           # تنسيقات لوحة التحكم
├── js/
│   ├── data.js             # البيانات + دوال النظام
│   ├── app.js              # منطق الموقع العام
│   └── admin.js            # منطق لوحة التحكم
└── images/                 # مجلد الصور
```

---

## 🔐 طرق الوصول إلى لوحة التحكم

### 1. عبر الرابط المباشر
افتح في المتصفح: `admin.html`

### 2. عبر اختصار لوحة المفاتيح
من داخل الموقع العام، اضغط: `Ctrl + Alt + A`

### 3. عبر رابط الفوتر
رابط خفي بلون شفاف في أسفل الصفحة

---

## 🎯 ميزات لوحة التحكم

### 1. الإحصائيات
- إجمالي العقارات
- العقارات المعتمدة
- العقارات المعلقة
- طلبات العملاء
- عقارات للبيع / للإيجار
- مخططات بيانية (حسب النوع، حسب المدينة)

### 2. إدارة العقارات
- عرض جميع العقارات في جدول
- البحث في العقارات
- فلترة حسب الحالة (معتمد/معلق)
- **تعديل** أي عقار
- **حذف** أي عقار
- **عرض** العقار في الموقع
- **موافقة** على العقارات المعلقة

### 3. العقارات المعلقة
- عرض العقارات بانتظار الموافقة
- عرض تفاصيل كاملة (مالك، هاتف، وصف)
- موافقة بنقرة واحدة
- تعديل قبل الموافقة
- رفض (حذف)

### 4. طلبات العملاء
- عرض جميع طلبات "اطلب عقارك"
- تفاصيل العميل (اسم، هاتف، بريد)
- التواصل المباشر (اتصال، واتساب، بريد)
- تغيير الحالة: جديد → تم التواصل → مغلق
- حذف الطلبات

### 5. إضافة عقار جديد
- نموذج كامل بنفس حقول الموقع
- العقار يُنشر مباشرة (معتمد)
- خيار "عقار مميز" للظهور في الواجهة

---

## 🔄 نظام العمل

### للعقارات المضافة من الموقع (الزوار):
1. الزائر يملأ نموذج "أضف عقارك"
2. العقار يُحفظ بحالة **"معلق"** (لا يظهر في الموقع)
3. رسالة تأكيد: "سيتم مراجعة العقار من قبل الإدارة"
4. المشرف يراجع العقار في لوحة التحكم
5. المشرف يوافق → العقار يظهر في الموقع
6. أو المشرف يحذف/يرفض العقار

### للعقارات المضافة من لوحة التحكم (المشرف):
1. المشرف يضيف العقار من "إضافة عقار"
2. العقار يُنشر **مباشرة** بحالة "معتمد"
3. يظهر في الموقع فوراً

---

## 💾 تخزين البيانات

جميع البيانات تُخزن في `localStorage` المتصفح:
- `properties` - جميع العقارات
- `propertyRequests` - طلبات العملاء
- `favorites` - العقارات المفضلة للزوار
- `adminSession` - جلسة المشرف (صالحة 24 ساعة)

### لتصدير البيانات:
- البيانات محفوظة في المتصفح فقط
- لمشاركة البيانات بين الأجهزة، يلزم نقل `localStorage`
- للإنتاج الفعلي، يُفضل استخدام backend (مثل PHP/Node.js) مع قاعدة بيانات

---

## 🎨 التخصيص

### تغيير بيانات الدخول:
افتح `js/data.js` وعدّل:
```js
const ADMIN_CREDENTIALS = {
    username: 'admin',     // غيّر اسم المستخدم
    password: 'admin123'   // غيّر كلمة المرور
};
```

### تغيير ألوان الموقع:
افتح `css/style.css` وغير المتغيرات:
```css
:root {
    --navy-900: #0a1f3d;    /* الكحلي الداكن */
    --gold-500: #c9a96e;    /* الذهبي */
    --beige-100: #faf7f0;   /* البيج الفاتح */
}
```

### إضافة مدن جديدة:
1. افتح `js/data.js` وأضف المدينة في `CITIES`
2. افتح `index.html` وأضفها في `<select>` و `chips`
3. افتح `admin.html` وأضفها في نماذج الإضافة والتعديل

---

## 📞 معلومات المكتب

- **الاسم:** مكتب أضواء الفيصلية للخدمات العقارية
- **العنوان:** مدينة المزاحمية - حي غرناطة - طريق الملك فيصل
- **الهاتف 1:** 0595305559
- **الهاتف 2:** 0509503345

---

## 🔒 الأمان وكلمة المرور

### ميزات الأمان المُطبقة:

- ✅ **تشفير كلمات المرور** باستخدام PBKDF2 (100,000 دورة) + Salt عشوائي
- ✅ **حد محاولات الدخول** (5 محاولات فاشلة → قفل 15 دقيقة)
- ✅ **Captcha بسيط** (جمع رقمين) ضد المحاولات الآلية
- ✅ **قفل تلقائي للجلسة** بعد 30 دقيقة من الخمول
- ✅ **Token جلسة آمن** (64 محرف عشوائي) عبر Web Crypto API
- ✅ **إجبار تغيير كلمة المرور** عند أول استخدام
- ✅ **سجل محاولات الدخول** (نجح/فشل/خروج) مع التاريخ
- ✅ **مؤشر قوة كلمة المرور** (5 مستويات)
- ✅ **متطلبات كلمة المرور القوية** (8+ أحرف، أحرف كبيرة/صغيرة، أرقام، رموز)
- ✅ **دعم عدة مشرفين** مع صلاحيات (superadmin/admin)
- ✅ **لا تُحفظ كلمات المرور** كنص واضح أبداً

### تغيير كلمة المرور:

1. سجّل الدخول للوحة التحكم
2. من القائمة الجانبية: **الأمان وكلمة المرور**
3. أدخل كلمة المرور الحالية والجديدة (8 أحرف على الأقل)
4. يجب أن تحتوي الجديدة على:
   - أحرف كبيرة وصغيرة
   - أرقام
   - رموز خاصة (يفضل)

### إضافة مشرف جديد (للمطورين):

من Console المتصفح في `admin.html`:
```javascript
await addAdmin('username', 'strongPassword123!', 'admin');
```

### للإنتاج الفعلي (تحسينات موصى بها):

- استخدم backend (Node.js/PHP) مع قاعدة بيانات حقيقية
- فعّل HTTPS
- أضف 2FA (TOTP عبر تطبيق Google Authenticator)
- راقب محاولات الدخول من IP عبر backend
- استخدم JWT مع refresh tokens
- أضف CSRF protection

---

## 📱 التوافق

- ✅ متجاوب مع جميع الأجهزة (موبايل، تابلت، ديسكتوب)
- ✅ يعمل على جميع المتصفحات الحديثة
- ✅ دعم كامل للغة العربية و RTL
- ✅ الخطوط: Cairo + Tajawal
- ✅ الأيقونات: Font Awesome 6

---

## 🛠️ التقنيات المستخدمة

- HTML5 + CSS3 (متغيرات، Grid، Flexbox)
- Vanilla JavaScript (بدون أي مكتبات)
- LocalStorage لتخزين البيانات
- Google Fonts للخطوط العربية
- Font Awesome للأيقونات

---

## 📞 الدعم

لأي استفسار أو مساعدة، يرجى التواصل مع مطور الموقع.
